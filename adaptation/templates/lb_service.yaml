apiVersion: v1
kind: Service
metadata:
  annotations:
    service.beta.kubernetes.io/azure-dns-label-name: {{ .Values.lbService.dnsname }}
  name: frontend-icap-lb
spec:
  type: LoadBalancer
  ports:
  - name: icap-port
    port: {{ .Values.lbService.nontlsport }}
    targetPort: {{ .Values.lbService.nontlsport }}
  - name: icap-service
    port: {{ .Values.lbService.tlsport }}
    targetPort: {{ .Values.lbService.tlsport }}
  selector:
    app: adaptation
