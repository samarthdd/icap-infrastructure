apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-transactionstore
  labels:
    usage: pv-transactionstore
spec:
  capacity:
    storage: {{ .Values.transactionstore.storage }}
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  azureFile:
    secretName: transactionqueryservicepv
    shareName: {{ .Values.transactionstore.shareName }}
    readOnly: false