{{- $root := . }}
{{- range $key, $value  := .Values.policymanagementapi.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $key }}
data:
  {{- range $subKey, $subValue  := $value }}
  {{ $subKey }}: {{ $subValue| b64enc }}
  {{- end }}
---
{{- end }}
