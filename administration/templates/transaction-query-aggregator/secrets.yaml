{{- $root := . }}
{{- range $name, $values := .Values.transactionqueryaggregator.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "transactionqueryaggregator.fullname" $root }}-{{ $name | lower }}
data:
  {{- range $key, $value := $values }}
  {{ $key }}: {{ $value | b64enc }}
  {{- end }}
---
{{- end }}
