apiVersion: v1
kind: Service
metadata:
  name: nginx-ingress-default-backend
  namespace: nginx
  labels:
    app: nginx-ingress
    component: default-backend
spec:
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: nginx-ingress
    component: default-backend
